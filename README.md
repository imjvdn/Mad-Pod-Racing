# CodinGame bots

Repository of CodinGame puzzle / contest bots.

## Layout

- `bots/<game>/bot.py`: single-file CodinGame submission (no local imports).
- `bots/<game>/README.md`: strategy notes + how to submit/run.
- `tools/pack.py`: optional “pack to stdout” helper (useful when you *do* add local modules later).

## Bots

- `bots/mad_pod_racing`: Mad Pod Racing (Gold league).


